# 강사계약 관리 사이트 - 설치 가이드

이 사이트는 Firebase(무료 요금제로 충분)와 GitHub Pages로 운영합니다. 서버 관리가 필요 없고, 지혜예유님이 이미 사용 중이신 다른 웹앱들(문제은행, 상담 예약장 등)과 같은 방식입니다.

전체 과정은 처음이어도 30~40분 정도면 끝납니다. 순서대로만 따라오시면 됩니다.

---

## 1. Firebase 프로젝트 만들기

1. https://console.firebase.google.com 접속 → Google 계정으로 로그인
2. "프로젝트 추가" 클릭 → 프로젝트 이름 입력 (예: `gml-instructor-contract`) → 계속 진행 (Google Analytics는 꺼도 무방)
3. 프로젝트가 생성되면 왼쪽 메뉴 톱니바퀴(프로젝트 설정) → "일반" 탭 → 아래로 스크롤 → "내 앱" → `</>` (웹) 아이콘 클릭
4. 앱 닉네임 아무거나 입력 (예: `contract-site`) → "앱 등록"
5. 화면에 나오는 `firebaseConfig = { apiKey: ..., authDomain: ..., ... }` 값을 통째로 복사

## 2. 사이트에 Firebase 설정 붙여넣기

1. 이 폴더의 `assets/firebase-config.js` 파일을 엽니다.
2. `여기에_...` 로 되어 있는 부분을 1번에서 복사한 실제 값으로 교체하고 저장합니다.

## 3. Firestore(데이터베이스) 켜기

1. Firebase 콘솔 왼쪽 메뉴 → "Firestore Database" → "데이터베이스 만들기"
2. 위치는 `asia-northeast3 (서울)` 선택 → "프로덕션 모드"로 시작 (규칙은 4단계에서 바로 교체합니다)
3. 생성이 끝나면 상단 "규칙" 탭 클릭 → 이 폴더의 `firestore.rules` 파일 내용을 전체 복사해서 붙여넣기 → "게시" 클릭

이 규칙 덕분에 강사가 입력한 주민등록번호·계좌정보는 **관리자로 로그인한 사람만** 조회할 수 있고, 강사 본인도 제출 후 다시 열람할 수 없습니다(링크 유출 시에도 개인정보가 노출되지 않도록 하기 위함입니다).

## 4. 관리자 로그인(Authentication) 켜기

1. Firebase 콘솔 왼쪽 메뉴 → "Authentication" → "시작하기"
2. "로그인 방법" 탭 → "이메일/비밀번호" 선택 → 사용 설정 켜기 → 저장
3. "Users" 탭 → "사용자 추가" → 관리자로 쓸 이메일과 비밀번호 입력 (이 계정이 곧 "관리자 로그인" 계정입니다)
   - 여러 명이 관리자로 쓸 경우 이 단계에서 계정을 추가로 만들면 됩니다.
   - ⚠️ 여기서 만든 계정만 사이트 관리자로 로그인할 수 있고, 이 외에는 회원가입 기능 자체가 없으니 안심하셔도 됩니다.

## 5. GitHub Pages로 배포하기

기존에 쓰시던 방식과 동일합니다.

1. GitHub에 새 저장소(Repository)를 만듭니다 (예: `instructor-contract-site`)
2. 이 폴더(지금 전달드린 압축 파일 압축 해제한 내용) 전체를 저장소에 업로드합니다.
3. 저장소 "Settings" → "Pages" → Source를 "Deploy from a branch" → Branch를 `main` / `/(root)` 로 설정 → Save
4. 몇 분 후 `https://<깃허브아이디>.github.io/<저장소이름>/` 주소로 접속하면 관리자 로그인 화면이 나타납니다.

## 6. 실제 사용 흐름

1. `index.html`(사이트 첫 화면)에서 4단계에서 만든 관리자 계정으로 로그인
2. "회사(발주처) 정보 설정"에서 회사명·사업자등록번호·대표자명 등을 한 번만 입력 (모든 계약서의 "갑" 정보로 자동 반영됩니다)
3. "+ 새 프로그램 만들기"로 프로그램명/일시/강사비/출장여비/기타를 입력 → 생성되면 강사 입력용 링크가 하나 나옵니다
4. 이 링크를 해당 프로그램에 참여하는 강사님들께 전달 (문자, 카카오톡, 이메일 등 무엇이든 가능하며, 여러 명이 같은 링크로 각자 입력합니다)
5. 강사가 링크에 접속해 이름·주민등록번호·계좌정보·원천징수 구분을 입력하고 화면에 직접 서명 후 제출하면, 제출 즉시 강사 본인에게도 계약서 PDF 다운로드/인쇄 버튼이 제공됩니다
6. 관리자는 "상세보기"에서 프로그램별 제출 현황을 한 번에 확인하고, "엑셀 다운로드"로 세무서 제출/내부 정리용 파일을 받을 수 있으며, 강사별 계약서 PDF도 개별 다운로드할 수 있습니다
7. 동시에 여러 프로그램을 운영해도 되며, 프로그램 목록 화면이 게시판처럼 계속 쌓입니다. 강의가 끝난 프로그램은 "마감"을 눌러 더 이상 입력받지 않도록 닫을 수 있습니다.

---

## 이번 버전에서 제가 임의로 정한 부분 (다르게 하고 싶으시면 언제든 말씀해주세요)

- **계약서 서식**: 정해주신 기존 양식이 없어 일반적인 강사 위촉 계약서 조항(목적/용역내용/강사료·원천징수/개인정보 수집·이용/해지/기타)으로 기본 틀을 만들었습니다. `assets/contract.js`의 `renderContractHTML` 함수 안 문구를 자유롭게 수정하실 수 있고, 실제 사용 전 계약서 문구는 세무사·노무사 등 전문가 확인을 한 번 받아보시길 권해드려요.
- **관리자 로그인 방식**: 주민등록번호·계좌정보처럼 민감한 정보를 다루기 때문에, 기존에 쓰시던 "간단한 비밀번호" 대신 이메일/비밀번호 로그인(Firebase Authentication)으로 만들었습니다. 이렇게 해야 Firestore 보안 규칙에서 "로그인한 사람만 조회 가능"을 실제로 강제할 수 있습니다.
- **서명 방식**: 실제 사인을 인식하는 전자서명 프로그램(모두싸인 등)까지는 아니고, 화면에 손가락/마우스로 그리는 간이 서명(캔버스)으로 구현했습니다. 법적 효력이 중요한 계약이라면 모두싸인 같은 전자계약 서비스 연동을 별도로 검토해보시는 것도 추천드립니다.

## 보안/개인정보 관련 참고사항

- 주민등록번호는 세무 신고(원천징수) 목적으로만 수집·이용되도록 안내 문구를 넣었습니다. 실제 운영 시 사용 목적 외 보관하지 않도록 주기적으로 지난 프로그램의 제출 데이터를 정리(삭제)하시는 것을 권장드립니다.
- 관리자 계정 비밀번호는 다른 사람과 공유하지 마시고, 사용하지 않는 관리자 계정은 Firebase 콘솔에서 바로 삭제하실 수 있습니다.
- 이 가이드의 보안 규칙은 "관리자만 개인정보를 열람"하는 수준의 기본적인 보호이며, 법적으로 요구되는 수준의 암호화·접근통제가 필요한 경우(예: 대량의 주민등록번호를 장기간 보관) 별도의 전문가 검토가 필요할 수 있습니다.
